{
  "mcpServers": {
    "fff-router": {
      "command": "bun",
      "args": ["../bin/fff-router-mcp.ts"],
      "lifecycle": "ephemeral",
      "description": "fff-router MCP target for wrapper and Pi integration."
    }
  }
}
