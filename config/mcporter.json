{
  "mcpServers": {
    "fff-router": {
      "command": "bun",
      "args": ["../bin/fff-router-mcp.ts"],
      "lifecycle": "ephemeral",
      "description": "Compatibility stdio MCP target that proxies to the HTTP daemon."
    }
  }
}
